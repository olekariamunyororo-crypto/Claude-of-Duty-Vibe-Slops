# Optional .glb models — drop them EXACTLY here

Valid single-cube placeholder GLBs ship with these names (loader flags <100 tris as
placeholder and uses procedural stand-ins). Replace for real art:

1) yacht.glb — Frickie's Yacht
2) nv4.glb — Custom Carbine NV4 (COD)
3) ghost.glb — optional local override

Default Ghost body loads from Thrixel (game_ready_military_character):
  https://api.thrixel.com/api/v1/994256bc-d57f-4d1e-92ba-32020aceda22/download?format=glb

Live swaps (IndexedDB, no rebuild):
  Menu → CREDITS / MODEL SWAP → Yacht / NV-4 / Ghost uploaders.

No Draco compression. Respect each author's license.
